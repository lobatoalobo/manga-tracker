import { StoreHome } from "@/components/store-home/StoreHome";

export const metadata = { title: "Inicio · Tienda · Nakama" };

/** Home de la tienda (estructura visual con datos mock; sin funcionalidad ni API todavía). */
export default function StoreHomePage() {
  return <StoreHome />;
}
