import { PreventasScreen } from "@/components/store-preventas/PreventasScreen";

export const metadata = { title: "Preventas · Tienda · Nakama" };

/** Home del módulo Preventas (estructura visual con datos mock; sin funcionalidad ni API todavía). */
export default function PreventasPage() {
  return <PreventasScreen />;
}
