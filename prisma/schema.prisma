generator client {
  provider = "prisma-client"
  output   = "../lib/generated/prisma"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Manga {
  id           Int    @id
  title        String
  coverImage   String
  totalVolumes Int
  ownedVolumes String
}